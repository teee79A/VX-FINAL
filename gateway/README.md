# gateway

This is the only public VXSTATION surface.

All client traffic must enter through this gateway path:

`client -> gateway/api + gateway/server -> internal routing -> kitty rooms + engines`

No room is exposed directly.
